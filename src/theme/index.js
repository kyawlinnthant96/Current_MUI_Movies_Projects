import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    "navLink": {
      fontSize: "1rem",
      color: "#1E1EA1"
    },
    "active-navLink": {
      fontSize: "1rem",
      color: "#00F"
    }
  }
})